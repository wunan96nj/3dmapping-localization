MacOS环境下：
brew install cv
brew install 安装替换掉 libgomp.1.dylib

Idea环境下：
GIT clone后，如果有异常，则删除.idea文件夹，重新打开工程
缺少的包需要在IDEA的Terminal中安装
(venv) workstation101-2:py-3dmap-rest-gate akui$
/Users/akui/eclipse-workspace/py-3dmap-rest-gate/venv/bin/python -m pip install --upgrade pip
pip3 install flask
pip3 install flask-restful
pip3 install opencv-python
pip3 install scipy
pip3 install open3d



